import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afriagrocore.app',
  appName: 'AfriAgroCore',
  webDir: 'dist/public'
};

export default config;
